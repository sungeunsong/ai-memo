# Android Share Intent 설계

## 1. 목적

유튜브, 인스타그램 등 외부 앱의 공유 시트에서 우리 앱이 공유 대상에 나타나고,
공유된 URL 또는 텍스트를 받아서 즉시 로컬에 저장하는 구조를 정의한다.

특히 인스타그램 게시물/릴스, DM에서 복사한 텍스트, Notion/Google Docs 링크처럼
흩어지기 쉬운 정보를 capture inbox에 모으는 것이 우선 목적이다.

핵심 원칙:
- 공유 진입도 일반 저장과 동일하게 `저장 우선`
- 공유 데이터는 먼저 로컬 저장
- AI/메타데이터 보강은 저장 이후 비동기 처리

---

## 2. 지원 우선순위

1. 유튜브 링크 공유
2. 인스타그램 링크 공유
3. Notion / Google Docs / Google Drive 링크 공유
4. 일반 웹 URL 공유
5. 텍스트 안에 포함된 URL 공유
6. URL 없는 텍스트 공유

초기 범위에서는 `text/*` 공유를 우선 처리한다.

이유:
- 유튜브/인스타 공유는 실제로 URL 또는 URL이 포함된 텍스트 형태가 많다
- DM/캡션 복사본은 URL 없이 텍스트만 들어올 수도 있다
- 이미지/동영상 직접 공유보다 링크 저장이 제품 핵심과 더 가깝다

편의성 관점에서 공유 시트만 유일한 진입점으로 보면 안 된다. Instagram DM이나 일부 앱 화면에서는 메시지 전체 공유가 제한될 수 있으므로, 사용자가 링크를 길게 눌러 공유하거나 복사한 뒤 앱을 열어 저장하는 흐름도 같은 1차 경로로 취급한다.

---

## 3. Android 동작 방식

Android 공식 방식은 `ACTION_SEND` 또는 `ACTION_SEND_MULTIPLE` intent를
manifest의 `intent-filter`로 수신하는 것이다.

우리 앱에서 필요한 최소 범위:
- `ACTION_SEND`
- MIME type: `text/*`
- 필요 시 후속 단계에서 `image/*`, `video/*` 추가 검토

공식 참고:
- Android Developers: Receive simple data from other apps

---

## 4. Expo 기준 구현 전략

### 추천 접근
- Expo SDK 54 기준 `expo-share-intent` 사용

이유:
- Expo managed / prebuild 흐름에서 Android share intent를 다루기 가장 현실적임
- text/url 중심 시작에 적합
- 이후 iOS share extension까지 같은 개념으로 확장 가능

주의:
- Expo Go로는 테스트 불가
- Custom Dev Client 또는 실제 빌드 필요
- 실제 도입 시 `expo-linking`, plugin 설정, dev build 절차가 필요

현재 단계 결론:
- `expo-share-intent` 패키지와 app config plugin은 도입됨
- Android `text/*` intent filter가 설정됨
- 실제 유튜브/인스타 앱 공유 테스트는 dev build와 실기기에서 추가 검증 필요

---

## 5. 앱 내부 데이터 흐름

공유 진입 시 흐름:

1. 외부 앱이 `text/plain` 공유
2. Android share intent가 앱을 실행
3. 공유 payload에서 URL 후보 추출
4. URL이 있으면 정규화하고 여러 URL 후보를 보존
5. URL이 없어도 원문 텍스트를 저장
6. source type 자동 분류
7. 로컬 DB 저장
8. 목록 즉시 반영
9. 메타데이터 보강
10. 이후 AI/동기화 진행

최종 구조:

`Share Intent -> Shared Text Normalizer -> Source Classifier -> Local Save -> Metadata Enrichment -> AI -> Sync`

복사 후 앱 실행 흐름:

1. 사용자가 DM/캡션/문서 링크를 복사
2. 앱을 실행
3. 앱이 클립보드에서 URL 또는 저장 가능한 텍스트 후보를 감지
4. 첫 화면 상단에 "복사한 내용 저장" 제안 표시
5. 사용자가 확인하면 공유 인입과 같은 저장 파이프라인 사용

`Clipboard Candidate -> User Confirm -> Shared Text Normalizer -> Source Classifier -> Local Save`

---

## 6. 현재 코드 기준 준비된 부분

이미 준비된 것:
- URL 정규화 저장 흐름
- 로컬 DB 저장
- 저장 후 목록 즉시 반영
- 저장 후 메타데이터 비동기 보강
- Android `text/*` share intent 패키지 설정
- 공유 payload를 현재 저장 흐름에 연결

이번에 추가된 준비:
- 공유 텍스트 안에서 URL을 추출하는 로직
- 즉, `"이거 봐봐 https://youtube.com/..."` 형태도 저장 가능

관련 코드:
- `apps/mobile/src/features/capture/normalizeSharedInput.ts`
- `apps/mobile/src/features/capture/shareIntent.ts`

---

## 7. 유튜브/인스타 대응 원칙

### 유튜브
- `youtube.com/watch`
- `youtu.be/...`
- `youtube.com/shorts/...`

처리:
- URL 정규화
- 가능하면 `watch?v=` 형태로 통일
- oEmbed + HTML meta로 제목/요약/썸네일 확보

### 인스타그램
- `instagram.com/p/...`
- `instagram.com/reel/...`
- `instagram.com/reels/...`

처리:
- URL 정규화
- 추적 파라미터 제거
- 공개 페이지 HTML의 `og:*` 메타 우선 사용

### Notion / Google Docs / Google Drive
- `notion.so`, `notion.site`
- `docs.google.com/document`
- `docs.google.com/spreadsheets`
- `docs.google.com/forms`
- `forms.gle`
- `drive.google.com`

처리:
- source type을 일반 웹과 구분
- 문서 링크로 표시
- 공개 메타데이터가 부족해도 원문 텍스트와 URL은 보존

---

## 8. 실제 구현 단계

### Step 1 완료
- Android용 share intent 패키지 도입
- app config plugin 연결
- `text/*` intent filter 활성화

### Step 2 완료
- 앱 시작 시 공유 payload 읽기
- payload의 `text`, `webUrl`을 현재 저장 로직에 연결

### Step 3
- 공유로 들어왔을 때 전용 진입 UX 추가
- "공유된 링크를 저장합니다" 확인 화면 또는 자동 저장 UX 결정

### Step 4
- 유튜브/인스타 실제 기기 테스트
- 예외 케이스 정리

### Step 5
- URL 없는 텍스트 저장 지원
- 여러 URL 추출 결과 저장
- source type 자동 분류 추가

---

## 9. 실제 사용 기준 UX 개선안

현재 구조는 기술적으로는 `저장 우선` 원칙에 맞지만, 실제 사용자가 느끼는 흐름에서는
다음 문제가 생기기 쉽다.

- 공유 후 앱으로 넘어왔을 때 화면 전체가 일반 홈과 동일해서, 사용자가 지금 공유 저장 플로우에 있는지 바로 알기 어렵다.
- 자동 저장은 빠르지만, 어떤 링크/텍스트가 저장됐는지 확인할 수 있는 표면이 토스트와 리스트 하이라이트에 의존한다.
- 클립보드 저장 제안이 계속 뜨면 편리함보다 피로도가 커질 수 있다.
- 여러 URL이 포함된 텍스트를 저장할 때 사용자는 “하나만 저장됐는지, 전부 보존됐는지”를 알기 어렵다.
- URL 없는 긴 텍스트 저장은 제목/요약이 나오기 전까지 무엇인지 식별하기 어렵다.
- 모바일에서는 저장 직후 상세가 바텀시트로 뜨거나 리스트 최상단으로 이동하는 규칙이 일관돼야 한다.

### UX 원칙

1. 저장은 즉시 끝낸다.
2. 저장 후에는 사용자가 저장 결과를 1초 안에 확인할 수 있어야 한다.
3. 공유 진입과 일반 앱 실행 진입은 화면 상태가 달라야 한다.
4. AI/메타데이터 보강은 부가 처리로 보이고, 저장 성공 여부와 섞이지 않아야 한다.
5. 클립보드는 사용자가 통제한다고 느껴야 한다.
6. 실패해도 원문은 잃지 않아야 하며, 실패 이유는 짧게 알려야 한다.

### 권장 공유 진입 UX

공유 시트에서 우리 앱을 선택한 뒤에는 전용 저장 상태를 먼저 보여준다.

권장 흐름:

1. 앱 실행
2. 상단에 공유 저장 미니 패널 표시
3. 즉시 로컬 저장
4. 저장 성공 시 새 항목 프리뷰 표시
5. 1.5~2초 뒤 Inbox 목록으로 자연스럽게 편입

권장 문구:

- 저장 중: `공유한 내용을 저장하는 중`
- 저장 완료: `수집함에 저장됨`
- URL 추출 완료: `링크 2개와 원문을 함께 보관했어요`
- 텍스트 저장 완료: `텍스트 메모로 저장됨`
- 파일 미지원: `파일은 아직 지원하지 않아요. 링크나 텍스트를 공유해 주세요.`

공유 저장 패널에는 최소한 다음 정보가 있어야 한다.

- 출처 앱 또는 저장 경로: `공유로 저장`
- 저장 대상 미리보기: 도메인, 첫 URL, 원문 첫 줄
- 저장 상태: `저장 중`, `저장됨`, `AI 정리 중`, `정리 실패`
- 주요 액션: `열기`, `메모 추가`, `닫기`

자동 저장 후 곧바로 상세 바텀시트를 띄우는 방식은 기본값으로 두지 않는 편이 낫다.
공유 저장은 사용자가 원래 앱으로 빨리 돌아가려는 상황이 많기 때문이다.
대신 저장 완료 패널에서 `열기`를 누르면 상세를 열고, 아무 조작이 없으면 목록 최상단에만 반영한다.

### 권장 클립보드 UX

클립보드 후보 감지는 유용하지만, 다음 조건을 둬야 한다.

- 같은 클립보드는 한 번 닫으면 일정 시간 다시 제안하지 않는다.
- 앱 실행 직후 한 번만 강하게 제안하고, 이후에는 작은 배너 또는 입력창 보조 액션으로 낮춘다.
- URL이면 도메인과 제목 후보를 보여준다.
- 텍스트면 첫 줄과 글자 수를 보여준다.
- 저장 전 사용자가 원문을 확인할 수 있어야 한다.

권장 배너 구조:

`복사한 내용 저장할까요?`

- 보조 정보: `instagram.com 링크`, `텍스트 128자`, `링크 3개 포함`
- 액션: `저장`, `미리보기`, `무시`

`닫기`보다 `무시`가 낫다. 사용자가 “이번 후보를 저장하지 않는다”는 의미를 더 명확히 이해할 수 있다.

### 여러 URL 저장 UX

여러 URL이 포함된 입력은 단일 항목 안에 원문과 링크 목록을 보존하되, 사용자가 저장 결과를 이해할 수 있게 표시한다.

목록 카드:

- 제목: 원문 첫 줄 또는 대표 링크 제목
- 보조 문구: `링크 3개 포함`
- 출처 배지: 대표 source type

상세 화면:

- 원문 섹션
- 대표 링크
- 추출된 링크 목록
- 각 링크의 도메인/타입 배지

추후 사용자가 원하면 `각 링크를 개별 항목으로 분리 저장` 액션을 추가할 수 있다.
초기 MVP에서는 자동 분리보다 원문 보존을 우선한다.

### URL 없는 텍스트 저장 UX

텍스트만 저장되는 경우에는 링크 카드처럼 보이면 안 된다.

목록 카드:

- 제목: 원문 첫 줄 기반
- 보조 문구: `텍스트 메모`
- 썸네일 영역 없음
- AI 정리 전 상태: `요약 준비 중`

상세 화면:

- 원문을 상단 가까이에 배치
- `퀵 메모`보다 `원문`이 먼저 보여야 한다
- AI 요약은 원문 아래에 둔다

사용자는 텍스트를 저장한 직후 “내가 복사한 내용이 맞는지”를 확인하려고 한다.
따라서 URL 없는 텍스트에서는 AI 결과보다 원문 확인이 우선이다.

### 저장 직후 상태 디자인

저장 직후에는 다음 3단계를 분리해서 보여준다.

1. `로컬 저장 완료`
2. `AI 정리 중`
3. `동기화 대기/완료`

사용자 입장에서는 1번만 성공하면 핵심 작업은 끝난 것이다.
따라서 AI 실패나 동기화 실패가 저장 실패처럼 보이면 안 된다.

권장 상태 표시:

- 카드 상단 또는 우측 작은 배지: `저장됨`
- AI 상태: `정리 중`, `정리 완료`, `정리 실패`
- 동기화 상태: 상세 또는 설정성 정보로 낮게 표시

홈 상단의 `원격 동기화 완료`는 일반 사용자에게 우선순위가 낮다.
MVP에서는 `저장됨`, `정리 중` 같은 작업 중심 상태가 더 중요하다.

### 홈 화면 정보 구조 개선

현재 홈은 입력, 클립보드, 검색, 카테고리, 리스트, 상세가 한 화면에 모두 올라와 있어
실제 모바일 사용에서는 저장 행동과 다시 찾기 행동이 섞인다.

권장 구조:

1. 상단: 빠른 저장 입력
2. 조건부: 공유/클립보드 후보 패널
3. 탭: `Inbox`, `정리됨`, `실패`
4. 검색
5. 최근 저장 목록

카테고리 탭은 저장 항목이 충분히 쌓인 뒤 의미가 커진다.
초기에는 고정 카테고리보다 `최근`, `AI 정리 중`, `링크`, `텍스트` 같은 상태/형태 필터가 더 실용적이다.

### 시각 디자인 방향

Capture inbox는 반복적으로 쓰는 생산성 도구이므로, 과한 장식보다 빠른 인지가 중요하다.

개선 방향:

- 네온/글로우 장식은 줄이고, 저장 대상 카드와 상태 배지를 더 선명하게 만든다.
- 이모지는 카테고리 보조 수준으로 제한한다.
- 버튼 문구는 `수집하기`보다 `저장`을 기본으로 쓴다.
- 동기화/AI 같은 내부 처리 용어는 사용자가 필요할 때만 노출한다.
- 모바일에서는 카드 높이와 정보 밀도를 일정하게 유지한다.

권장 주요 문구:

- 입력 placeholder: `링크나 텍스트 붙여넣기`
- 저장 버튼: `저장`
- 클립보드 배너: `복사한 내용 저장할까요?`
- 공유 저장 완료: `수집함에 저장됨`
- AI 진행: `요약 정리 중`
- AI 실패: `요약을 만들지 못했어요. 원문은 저장되어 있습니다.`

---

## 10. 테스트 케이스

1. 유튜브 앱에서 영상 공유 -> 우리 앱 선택 -> 링크 저장
2. 인스타 앱에서 릴스 공유 -> 우리 앱 선택 -> 링크 저장
3. 브라우저에서 URL 공유 -> 우리 앱 선택 -> 링크 저장
4. 텍스트 + URL 형태 공유 -> URL만 추출해 저장
5. URL 없는 텍스트 공유 -> 원문 텍스트 항목으로 저장
6. DM 복사 텍스트 + Notion 링크 -> 원문과 링크 모두 저장
7. Google Docs 링크 공유 -> 문서 링크로 분류
8. Instagram DM의 링크 길게 누르기 -> 공유 시트에 우리 앱 표시 여부 확인
9. Instagram DM의 링크 복사 -> 앱 실행 -> 클립보드 저장 제안 표시
10. Instagram DM 메시지 전체 복사 -> 앱 실행 -> 원문 텍스트 저장 제안 표시

---

## 11. UX 우선순위

실제 사용 기준으로는 다음 순서가 가장 효과적이다.

1. 공유/클립보드 저장 완료 패널 추가
2. 저장 성공, AI 정리, 동기화 상태 분리
3. URL 없는 텍스트 상세에서 원문 우선 배치
4. 여러 URL 포함 입력의 저장 결과 표시 개선
5. 클립보드 후보 무시/재노출 방지
6. 홈 화면에서 동기화 정보 우선순위 낮추기
7. 고정 카테고리 탭을 상태/형태 필터 중심으로 재검토

---

## 12. 다음 액션

다음 구현 작업:

1. 공유 저장 전용 미니 패널 구현
2. 클립보드 후보 배너 문구와 액션을 `저장/미리보기/무시`로 변경
3. 저장 직후 새 항목 프리뷰와 `열기`, `메모 추가` 액션 추가
4. URL 없는 텍스트 상세 화면에서 원문 섹션을 AI 요약보다 위로 이동
5. 여러 URL 저장 카드에 `링크 N개 포함` 표시
6. `저장됨`, `요약 정리 중`, `동기화 대기` 상태 배지 분리
7. Instagram DM 길게 누르기/공유/복사 실기기 테스트
