# TODO

> 2026-09-11 전면 재작성. 완료된 항목은 전부 걷어내고 앞으로 할 일만 남겼습니다.
> 지금까지 만든 것은 커밋 이력과 `Handoff_*.md`를 보면 됩니다.

## 지금 바로

비어 있습니다. 키 노출은 2026-09-16에 완전히 닫혔습니다.

- [x] 노출돼 있던 옛 Gemini 키 폐기 — 그 키로 직접 찔러 401을 확인했습니다
- [x] 안드로이드 실기기에서 프록시 경로 확인
      · 앱을 껐다 켜도 익명 계정이 늘지 않고, 공유 → 정리까지 요약이 붙습니다
      · 폴리필만으로는 부족했습니다. supabase-js는 저장소를 안 넘기면 '브라우저인가'를
        먼저 보는데, 그 판단이 `window`와 `document`가 **둘 다** 있는지라 리액트
        네이티브는 통과하지 못합니다. 저장소를 직접 넘겨야 합니다

> 2026-09-15: 웹 재배포 금지를 풉니다. 걸어둔 조건(프록시 완성)이 충족됐습니다.
> 앞으로 웹 배포는 CLAUDE.md대로 **요청받았을 때만** 합니다.

## AI 프록시 — 완료

**`/generate`까지 끝나 키 노출은 해소됐습니다** (2026-09-15).
`/read`는 옮기지 않기로 했습니다 — 아래 '`/read`를 옮기지 않는 이유' 참고.
남은 것은 계측과 못 재본 회귀 항목들입니다.

원래 이게 출시 관문이었던 이유: `EXPO_PUBLIC_*` 키는 빌드 시 번들에 인라인돼
웹 번들과 APK 양쪽에서 추출됩니다. 남에게 앱을 주는 것이 곧 키를 주는 것입니다.

설계 결론: **얇지만 검증하는 프록시.** 프롬프트와 사전(Registry)은 앱이 계속 소유하고,
API 키·모델·출력 상한·스키마는 서버가 소유합니다. V2 프롬프트 동작을 건드리지 않는 게
1차 목표라 프롬프트 서버 이관은 하지 않습니다.

- [x] Supabase 프로젝트 생성
- [x] `@supabase/supabase-js` + 익명 로그인(Anonymous Auth)
  - [x] web: 브라우저 localStorage (기본값) / android: `expo-sqlite/localStorage/install`
        — 새 의존성 없음. SecureStore와 2048바이트 청킹 문제를 통째로 피함
  - [x] AppState 연동 `startAutoRefresh` / `stopAutoRefresh` (없으면 복귀 시 401)
- [x] Edge Function — `/generate` 하나만 만들었습니다 (`/read`는 아래 참고)
  - [x] "Supabase 키가 있나"가 아니라 **실제 로그인된 user JWT인가**를 검증
        — `auth.getUser()`로 실제 사용자를 읽어 확인합니다. anon 키도 이 프로젝트가
        서명한 멀쩡한 JWT라 그것만 검사하면 번들에서 키를 뽑은 누구나 통과합니다
- [x] `items.enrich_request_id` — **한 번의 정리 작업 전체 ID** (read + generate 공통)
  - [x] 정리 시작 시 발급·저장, **전체 성공 후에만** 비움
  - [x] 앱이 죽었다 살아나면 회수(`staleEnrich`)가 **같은 ID로** 재요청 → 중복 과금 없음
  - [x] 새 ID가 기본. 물려받는 것은 **끊긴 것을 이어서 할 때뿐**
        (조각을 붙였거나 재분석을 누른 경우는 AI에 넣는 재료가 달라진 것이라,
        물려받으면 서버가 바뀐 줄 모르고 예전 결과를 돌려줍니다)
  - [x] 안드로이드 SQLite와 웹 localStorage 양쪽 스키마
  - [x] **백업에서는 제외** (`syncStatus`와 같은 이유 — 기기 사정이라 옮기면 안 됨)
- [x] `ai_requests` 테이블 — 멱등성과 계측을 겸함
  - [x] `UNIQUE(user_id, request_id, operation)` (기본키로)
  - [x] 행은 영구 보관(월 사용량 분석용), `result_json`만 기간 후 비움
  - [x] 보관 기간은 operation별로: read 24시간(용량 큼) / generate 7일(돈 드는 쪽)
  - [ ] 프롬프트 전문·Jina 원문은 장기 보관하지 않음
  - [ ] **`result_json`은 재시도·멱등성을 위한 임시 운영 캐시이지 개인 시렁의 영구
        클라우드 저장소가 아닙니다.** local-first 원칙과 충돌하지 않는다는 뜻입니다
- [x] `/generate` — 서버가 최종 Gemini body를 **재구성**
  - [x] `schemaId` allowlist (`content-v2`) — 클라이언트가 임의 스키마를 못 보내게.
        앱에서는 지웠습니다. 두 벌이 생기면 언젠가 어긋나고, 어긋난 쪽을 앱이 들고
        있으면 고치는 데 앱 재배포가 필요합니다
  - [x] 모델·`thinkingBudget`·출력 상한은 서버 환경변수로 고정
  - [x] 입력 길이·이미지 크기 상한 — 프롬프트 전체 길이로 막습니다. 본문만 떼어 재려면 프롬프트 형식을 서버가 알아야 해서, 정확한 절단은 앱이 하고 서버는 터무니없이 긴 것만 자릅니다
- [x] Gemini 키를 Supabase Secret에만 등록
- [x] 앱 호출을 `/generate`로 전환, `EXPO_PUBLIC_GEMINI_API_KEY` 제거
- [ ] `adapter.ts`의 `IS_ITEMS_SYNC_READY` — items 테이블·RLS가 생기면 켬.
      켜기 전에 `Authorization`을 anon 키에서 **로그인한 사용자 토큰**으로 바꿔야 함
      (지금대로면 `auth.uid()`가 비어 정책을 못 통과합니다)
- [x] atomic 하드캡 — **외부 호출 전에 1건 예약**, 예약 실패면 호출 안 함
  - [x] `UPDATE ... WHERE used < :cap RETURNING` 한 문장. 읽고-검사-쓰기는 뚫림
  - [x] read / generate 상한 분리 (비용원이 다름). 여기에 더해 **사용자 몫**과 **실제 호출 수**도 나눴습니다 — 재시도로 사용자 몫이 깎이면 앱이 죽은 대가를 사용자가 치릅니다. 전체 합산 상한도 추가(익명 계정은 무한정 만들 수 있어 1인당 상한이 계정 수만큼 곱해짐)
- [ ] 계측: 사용량 · 지연(read/generate 분리) · payload 크기
- [ ] 회귀 테스트
  - [x] HTTP 재시도 / 지수 백오프 — **앱이** 합니다. 서버가 자기 안에서 재시도하면
        실제 호출 세 번이 사용량 한 번으로 세어져 계수기가 실제와 어긋납니다
  - [x] **CORS** — 브라우저는 진짜 요청 앞에 OPTIONS를 보냅니다. curl 시험은 이 단계가
        없어 통과했고, 웹에서만 실패했습니다. **Edge Function을 새로 만들 때마다
        걸릴 자리입니다**
  - [~] **프록시 다운 시 로컬 요약기 폴백** — 프록시는 단일 장애점이 됨
        · 경로는 살아 있습니다. `fetchTextMetadataPatch`가 실패 시 본문 발췌로
          `digest`를 만들고 `aiStatus: 'failed'`로 남겨 재분석 버튼이 열립니다.
          일부러 프록시를 죽여놓고 확인한 적은 없습니다
  - [ ] 이미지(base64) 경로, 실제 payload 크기 실측
  - [ ] 12초 타임아웃 재측정 (홉이 하나 늘어남) — 지금은 30초로 잡아두었습니다
  - [ ] **앱 강제종료 후 같은 requestId로 회수되는지**
  - [x] 같은 requestId로 동시에 둘이 들어와도 바깥 호출은 한 번
  - [x] 상한 초과가 429로 떨어지는지

### 프록시를 붙이며 배운 것 (2026-09-16)

- **curl이 통과해도 브라우저는 막힐 수 있습니다.** 브라우저는 진짜 요청 앞에 OPTIONS를
  보내는데 curl에는 그 단계가 없습니다. 새 Edge Function마다 걸릴 자리입니다
- **`Deno.env.get(...)!`는 타입 검사만 속입니다.** 값이 없으면 undefined가 그대로 흘러가
  `models/undefined` 같은 요청이 나갑니다. 설정이 빠진 것이 호출 실패로 보입니다
- **환경변수 전파가 코드 배포보다 훨씬 느립니다.** 코드는 1분, 환경변수는 20분 넘게
  걸렸습니다. 그 사이 옛 값과 새 값이 섞여 응답해 재현이 들쭉날쭉했습니다.
  응답에 `build` 표시를 넣어둔 이유입니다
- **상한을 시험용으로 낮췄으면 되돌린 것을 반드시 확인할 것.** 되돌리기가 조용히
  실패해 실사용이 하루 1건으로 막혔습니다. `secrets list`의 해시는 값의 sha256이라
  `printf %s 50 | sha256sum`으로 대조할 수 있습니다
- [x] 웹 확인 — 저장 → 정리까지 돌려 `ai_requests`에 `succeeded`가 남는 것을 봤습니다
- [ ] 안드로이드 실기기 확인 (위 '지금 바로' 참고)
- [x] 번들에 secret 없는지 검사 후 웹 재배포
      · `.env`에서 키를 지웠으니 앞으로는 그 값으로 grep할 수 없습니다. 공개 서빙되는
        번들을 직접 받아 옛 키 값과 `generativelanguage.googleapis.com`으로 훑는
        방식이 남습니다. 둘 다 0건이어야 하고, `functions/v1/generate`는 있어야 합니다

### `/read`를 옮기지 않는 이유 (2026-09-15 결정)

원래는 `/generate`와 짝으로 옮길 생각이었습니다. 따져보니 **옮기는 쪽이 손해**입니다.

Gemini를 옮긴 이유는 셋이었습니다. 키가 있고, 돈이 나가고, 앱에 두면 도둑맞습니다.
**Jina는 셋 중 아무것도 해당하지 않습니다.**

옮기면 오히려 나빠지는 것:

- **속도 제한이 한곳에 몰립니다.** 지금은 사용자 폰마다 흩어져 나가는데, 서버로
  옮기면 전부 한 IP에서 나갑니다. 사용자가 늘수록 더 쉽게 걸립니다
- **우리가 사용자 URL을 다 보게 됩니다.** 지금은 폰에서 Jina로 바로 갑니다.
  안 봐도 되는 것을 보게 되는 건 개인정보 관점에서 손해입니다

옮길 논거로 생각했던 것들도 실제로는 약했습니다.

- *"회수가 싸진다"* — 앱이 이미 `cacheFetchedBodyIntoSource`로 긁은 본문을 조각에
  넣어둬 재정리는 네트워크 없이 됩니다. 정리 도중에 죽었을 때만 다시 긁는데 공짜입니다
- *"상한이 필요하다"* — 남용해도 그 사람 IP에서 나가고 우리 돈이 아닙니다.
  대신 불러주면 **우리 IP가 막힐** 위험만 생깁니다
- *"계측"* — 앱에서도 셉니다

**옮길 조건** (전부 '사용자가 늘면'입니다):

- [ ] Jina **유료 키**를 쓰게 될 때 — 속도 제한에 걸리거나, 품질·속도 때문에.
      **그 키는 반드시 서버에만 있어야 하므로 그 시점엔 선택이 아닙니다**
- [ ] 다른 리더로 갈아탈 때
- [ ] 사용자별 읽기 상한이 실제로 필요해질 때

`ai_requests`의 `operation = 'read'` 자리와 24시간 보관 기간은 그대로 둡니다.
쓰게 될 때 바로 얹을 수 있고, 비워둔다고 드는 비용이 없습니다.

**공개 출시 전으로 미룸**: CAPTCHA/Turnstile, IP rate limit, 사업 한도(무료/Pro 건수),
웹 로컬 데이터 유실 대응, SecureStore 청킹.
**비공개 테스트 단계에서는 계속 미뤄도 됩니다** — 아래 배포 섹션의 단계 구분 참고.
사업 한도는 **재지 않고 기록만** 하다가 숫자를 보고 따로 정합니다.

## 중복 저장 (2026-09-16 발견)

같은 링크를 두 번 공유하면 별개 저장물 두 개가 되고, **각각 따로 AI 정리가 돕니다.**
돈이 두 번 나가고 목록에도 같은 것이 둘 남습니다. 실사용 중에 우연히 드러났습니다.

TODO의 공유 항목에 '중복 감지'가 있는데 그건 *둘이 같은 것을 담는* 경우였습니다.
**본인이 두 번 담는 경우가 먼저이고, 그쪽이 훨씬 자주 일어납니다.**

- [x] 공유 직후 선택 시트에서 **"이미 담아둔 것 같다"고 알려주기**
      · 막지 않습니다. 아래 선택지는 그대로 두고 위에 후보를 짚어주기만 합니다
      · '기존 저장물에 합치기'가 원래 세 단계(합치기 → 목록 → 고르기)였는데,
        후보를 짚어주니 한 번에 눌러집니다
- [x] 무엇을 같다고 볼지 정하기 — `features/items/dedupe.ts`
      · 지울 꼬리표만 지웁니다(`stkn`·`igsh`·`utm_*`). `v=`처럼 **무엇을 가리키는지
        정하는 값은 건드리지 않습니다**
      · 한 저장물이 열쇠를 여럿 가집니다. 릴스에 DM을 붙였으면 조각의 링크로도
        찾아져야 합니다
      · 링크 없는 메모만 본문으로 비교합니다. 링크가 있는 것까지 본문으로 비교하면
        같은 링크를 저장한 메모끼리 엉뚱하게 묶입니다
- [x] 알려주기 전에 AI를 부르지 않기 — 시트가 뜨는 시점이 이미 `deferEnrich`라
      정리가 시작되기 전입니다

- [ ] 목록·검색에서도 중복을 알려줄지 — 지금은 담는 순간에만 짚어줍니다.
      이미 쌓인 중복은 손대지 않았습니다

## 실사용에서 나온 요구 (2026-09-16)

### 담기

- [ ] **`+` 수집 창에서 링크·글을 여러 개 한 번에** — 이미지는 이미 여러 장이 되는데
      글 입력은 하나뿐입니다
      · **정할 것: 여러 개를 별개 저장물로 담을지, 한 저장물의 조각으로 묶을지.**
        일이 완전히 달라집니다. 조각으로 묶으면 AI 호출 1회, 별개로 담으면 N회입니다

- [ ] 스크린샷 붙이는 20~40초 동안 무슨 일이 일어나는지 안 보임
      · 지금은 버튼만 돕니다. 실제로 "고장인가?" 하고 같은 장을 또 붙이는 일이
        있었습니다. 문구 시제는 고쳤지만 기다리는 동안은 여전히 깜깜합니다
      · `사진 옮기는 중 → 글자 읽는 중 (1/2) → 다시 정리하는 중` 정도는 필요합니다.
        store가 진행 상황을 화면에 흘려줘야 합니다

### 목록

- [ ] **정리 실패한 카드에서 바로 재분석** — 지금은 상세로 들어가야만 누를 수 있습니다
      · 실패는 드물지 않습니다(할당량·네트워크). 자주 쓰는 동작이 두 단계 뒤에 있습니다

### 공구

- [ ] **공구만 모아보기**
      · 지금 안 되는 이유가 분명합니다. `purchase_type`(구매 형태)이 `shopping`
        분야에만 달려 있고 `globalRole`도 없습니다. **`globalRole`이 있어야 분야를
        넘어 하나로 묶입니다**(`facets/extract.ts`). 여행 공구는 분야가 `travel`이라
        공구라는 사실이 어디에도 안 남습니다
      · 고칠 자리 둘: ① `purchase_type`에 `globalRole` 주기 ② 여행·맛집 글에서도
        그 항목이 뽑히게 하기. AI는 다른 분야의 항목도 실을 수 있지만(`facts[].domain`),
        프롬프트에 실리는 사전이 분야별로 잘려서 안 보일 수 있습니다
      · **공구는 분야가 아니라 축입니다.** 여행 공구도 맛집 공구도 있습니다

- [ ] **공구 분석 품질** — 품목·가격·마감일이 얼마나 정확히 잡히는지
      · 배포 후 관찰 항목(아래 '공구를 만들지 말지의 근거')과 같은 이야기입니다.
        개인 시렁에서는 틀려도 본인이 고치면 되지만, 공개 검색은 기준이 다릅니다

- [ ] **마감일에 시간까지** — 지금 `userDeadline`은 `YYYY-MM-DD`입니다
      · 공구는 '오늘 밤 11시 마감'이 흔합니다. 날짜만으로는 '오늘 마감'까지밖에 못 합니다
      · 양쪽 스키마와 `DeadlineEditor`, 그리고 AI 추출 형식까지 같이 바뀝니다
      · 시간이 없는 공구도 많으므로 **시간은 선택**이어야 합니다

- [ ] **알림** (공구 시작일·마감 임박) — 위 항목들이 끝난 뒤
      · 마감일 정확도가 전제입니다. 틀린 날짜로 알림이 오면 없느니만 못합니다

## 공유 (설계 확정, 구현 전)

설계 근거와 결정 이유는 메모리 `ai-memo-sharing-architecture` 참고.
원칙: **개인 시렁은 local-first 유지, 명시적으로 공유한 순간의 스냅샷만 서버로.**

> 인증은 AI 프록시 단계에서 이미 끝납니다. 여기서는 그 `auth.uid()`를 그대로 씁니다.

- [ ] RLS 정책 — 테이블 만들 때 같이. 빼먹으면 남의 데이터가 열립니다
- [ ] 공유 스냅샷 테이블 + Storage 버킷
- [ ] 읽기 전용 공유 URL (원본 DM 스크린샷은 **기본 제외** — 제3자 얼굴·이름이 찍혀 있음)
- [ ] 공유 링크 취소(revoke) + "공유 중인 목록" 화면
- [ ] 공유 페이지에서 `[내 시렁에 담기]` → 내 사전으로 매핑
- [ ] 중복 감지 (같은 것을 둘이 담으면 두 개가 됨)
      · **공구 DB의 전제조건이기도 합니다.** 같은 공구를 30명이 제보했을 때
        하나로 합치지 못하면 목록이 무너집니다. 합친 제보 수는 랭킹 신호 중 하나가 됩니다
- [ ] Collection — 서버는 entity, 화면은 facet 칩 (`collection:abc123` + 라벨)
  - [ ] 멤버·초대 링크·나가기·삭제 관리 모달
  - [ ] 스냅샷 수동 갱신 (write-then-swap: 업로드 성공 후 포인터 교체)
  - [ ] Storage 삭제 규칙 (기준은 '사람이 나감'이 아니라 'snapshot 제거')
  - [ ] 기본 목록엔 안 섞고, **검색 결과에는 별도 구획으로** 표시
- [ ] 익명 사용자 정리 정책 (생성일 기준 단순 삭제 금지. 초기엔 남용 방지만)

## 배포

`프록시 → 공유 → 공유 컬렉션 → 안드로이드 비공개 테스트 → 공개 출시 → 관찰 → iOS → 공구`

프록시는 끝났습니다. **다음은 공유입니다.**

**"출시"를 한 단어로 두지 않습니다.** 단계마다 붙는 일이 완전히 다릅니다.

### 1단계 — 안드로이드 비공개 테스트

- [ ] **Play Console에서 계정 유형과 생성일부터 확인**
      · 개인 계정이 **2023-11-13 이후 생성**됐다면, 프로덕션 액세스를 신청하기 전에
        **테스터 12명이 14일 연속** 참여하는 비공개 테스트를 거쳐야 합니다
      · 조직 계정이거나 그 이전 생성이면 해당하지 않습니다. 정책 문구는 계속 바뀌므로
        콘솔에서 현재 조건을 같이 확인할 것
- [ ] **테스터 모집 — 유료 대행 서비스로 한 번에** (2026-09-14 결정)
      · 직접 모으지 않습니다. 프록시·공유처럼 근본적인 것을 다 끝내고, 앱이 준비된
        시점에 유료 서비스로 채웁니다. 코드 만드는 동안 사람을 붙잡아둘 필요가 없습니다
      · **돈으로 줄어드는 건 모으는 수고이지 14일이 아닙니다.** 시계는 앱이 완성된 뒤에
        돌기 시작하므로 `공개 출시 = 프록시 완료 + 14일 + 심사`로 잡습니다
      · **15~20명** 여유 있게. 14일 내내 옵트인 상태여야 해서 중간 이탈이 카운트를 흔듭니다
      · 업체를 고를 때: Google은 **실제 테스트 활동**을 요구합니다. 옵트인만 찍고 빠지는
        방식은 프로덕션 액세스 심사에서 걸릴 수 있으니 진행 방식을 미리 확인할 것
- [ ] 이 단계에서는 미뤄둔 항목들(CAPTCHA·rate limit·계정 정리)을 계속 미뤄도 됩니다

### 2단계 — 안드로이드 공개 출시

- [ ] **개인정보처리방침** — 저장한 URL이 Jina Reader로, 내용이 Gemini로 전달되는 것을
      명시해야 합니다. 제3자 전송이라 빼놓을 수 없습니다
- [ ] 스토어 등재 자료 (스크린샷·설명·아이콘)
- [ ] 프록시 섹션에서 "공개 출시 전으로 미룸"에 넣어둔 항목들이 **여기서 전부 걸립니다**

### 3단계 — iOS (검증 직후 다음 순위)

"언젠가"로 두지 않습니다. **시렁은 공유 시트가 본체**라, 아이폰 사용자가 웹으로만
쓰면 복사-붙여넣기밖에 안 돼서 **제품을 반쪽만 경험**합니다. 그 상태가 굳으면
iOS 피드백이 구조적으로 왜곡됩니다. 자세한 작업은 아래 iOS 섹션 참고.

### 배포 후 관찰할 것 — 공구를 만들지 말지의 근거

공구 DB는 출시 시점부터 쌓이면 됩니다(공구는 신선도가 생명이라 과거 데이터는
어차피 만료입니다). 하지만 **공구를 만들 가치가 있는지 판단하는 데이터는 배포
직후부터 유효**하고, 이건 만료와 무관합니다.

- [ ] 전체 저장 중 `purchase_type = 공동구매` 비율
- [ ] 공구를 저장하는 사용자 비율 (몇 명에게 몰리는지, 고루 퍼지는지)
- [ ] 공구 저장이 몰리는 품목
- [ ] 마감일이 실제로 얼마나 잘 잡히는지 (공개 검색의 전제)
- [ ] 한 사용자가 반복적으로 공구를 저장하는지

지금 서버 DB를 만들 필요는 없고, **개인 저장물에 이 통계를 뽑을 구조가 남아 있으면
충분합니다.** 현재 V2가 이미 그렇습니다.

## 향후 — 계정 · 수익화 · 공구 (아직 설계 전)

지금 할 일은 없습니다. **`auth.uid()`를 모든 것의 기준점으로 두고
"사용자 = 기기"라고 가정하지 않는 것**만 지키면 나중에 다 붙습니다.
`profiles` / `entitlements` / `role` 테이블을 미리 만들지 마세요.

### 계정
- [ ] Anonymous → Google / Apple 계정 연결 (기존 uid와 공유 이력 유지)
- [ ] 연결된 계정 표시 · 로그아웃 · 회원 탈퇴
- [ ] 이메일/비밀번호 가입은 만들지 않음 (소셜 연결만)

### 수익화
- [ ] Pro entitlement + App Store / Play 구독 검증
- [ ] 일반 사용자 Pro 구독 → **인앱결제(IAP)**
- [ ] 인플루언서 홍보비 → **앱 밖(웹 파트너 포털)에서 받는 것을 전제로 설계**
      · 정책 회피가 목적이 아니라, **B2B 광고 거래에 필요한 정산·증빙(세금계산서 등)이
        스토어 결제 구조와 맞지 않을 가능성이 큽니다.** 인플루언서는 사업자라 홍보비를
        비용 처리해야 합니다. 다만 단정하지 말고 **세무·결제 구조를 별도 검토**할 것
      · 앱 내 노출을 파는 행위의 IAP 해당 여부도 각국 규제로 계속 바뀌므로,
        출시 형태가 정해지면 그 시점의 정책을 다시 확인

### 공구 (별도 큰 단계)
- [ ] 공개 공구 DB — 개인 시렁(local)과 **완전히 분리된 서버 영역**
  - [ ] `status` / `start_at` / `end_at` / `last_verified_at` / `source_url`
  - [ ] 마감된 공구도 **지우지 않음**(검색 기본 결과에서만 제외).
        가격 기준점이 되고, 같은 판매자의 재개설 주기가 드러나 "다음 공구 알림"의 근거가 됨
- [ ] 저장 시 **"공구 정보 공유" 옵트인** — 개인 저장이 자동으로 공개되지 않음
- [ ] 제보 병합(Entity Resolution) — 같은 공구 30건 제보 → 공구 1개 + 제보 30건
- [ ] 인플루언서 role · 프로필 · 리스팅 claim · 공구 등록(기간/가격/마감)
- [ ] **검색 상위 노출**을 파는 유료 프로모션 + 노출·클릭 성과 측정
  - [ ] **organic 순위와 sponsored를 절대 섞지 않음.** 광고는 명시 표기
- [ ] 공구 상세 → **외부 판매처 링크로 이동.** 시렁은 결제·배송·환불을 다루지 않음
- [ ] 공구 → `[내 시렁에 담기]`로 개인 아이템화 (공유 링크와 같은 동작 재사용)
- [ ] 정렬: 가격 낮은순 · 마감임박순 · 인기순 (홈은 시간순뿐이라 이건 새로 필요)

**왜 이 기능이 성립하나.** 인스타에도 Explore·검색·추천이 있어 비팔로우 공구를 못 보는 건
아닙니다. 문제는 다른 데 있습니다 — **인스타는 진행 중인 공구 전체를 재고 목록처럼
구조화해 검색·비교하게 해주지 않습니다.** 스토리는 24시간 뒤 사라지고, 마감일이
자연어로만 있어 정렬도 필터도 안 되고, 끝난 공구가 계속 걸립니다.
`유아매트 → 진행 중 5건 → 가격·마감 비교`가 되는 자리가 비어 있습니다.

**핵심 가설(미검증).** "팔로우한 사람 공구를 보고 산다"에 더해 **"필요한 상품의 공구를
직접 찾아본다"**는 패턴이 존재한다는 것. 지금은 개인 관찰에 기반한 제품 가설이지
시장 데이터로 확인된 게 아닙니다. 사용자가 생기면 "최근 공구 구매 경험"을 물어
(① 팔로우하던 사람이 해서 ② 필요한 걸 검색해서 ③ 지인이 보내줘서) 검증할 수 있습니다.
**②가 유의미하지 않으면 이 기능의 전제가 흔들립니다.**

**초기 공급원은 동의 기반 제보입니다.** `shopping` 분야가 품목·판매처·구매 형태·가격·
마감일을 이미 구조화해 뽑고 있어서, 사용자가 기여에 동의하면 공개 후보가 쌓입니다.
인플루언서에게 "올려달라"고 부탁할 필요가 없습니다. 쌓인 뒤 "당신 공구가 32명에게
저장됐다"고 알리면 claim하러 옵니다(옐프·구글 비즈니스 방식).
**단, 개인 저장이 자동으로 공개되는 구조는 만들지 않습니다.**

**벌티컬을 미리 자르지 않습니다.** 공급을 우리가 채우는 게 아니라 사용자 저장에서
받는 구조라, 카테고리를 제한하면 이미 들어온 데이터를 버리는 셈입니다. 전체를 받고
**데이터가 어디에 몰리는지 관찰**한 뒤, 수요 대비 커버리지가 충분해진 영역을
적극 노출합니다.

**탐색의 빈칸과 검색 0건은 다른 문제입니다.** 활성 공구가 없는 카테고리를 탐색 UI에서
숨기는 건 `SearchFilterBar`의 '죽은 칩 숨김'과 같은 원리로 해결됩니다. 하지만
`유아매트`를 검색해 0건을 보는 건 카테고리를 숨겨도 그대로입니다.

- [ ] **검색 0건율을 핵심 지표로 관리** (품목별 검색 수요 대비 결과 커버리지)
- [ ] **0건 검색어를 수집** — 이탈 신호가 아니라 **수요 신호**입니다.
      "유아매트 공구를 이번 달 40명이 찾았다"는 인플루언서 영업 재료가 됩니다
- [ ] 0건 화면에서 할 일: 마감된 공구 중 일치 건 제시(가격 기준점) +
      "이 품목 공구가 열리면 알림". **0건을 이탈이 아니라 수요 등록으로 바꿉니다**

**UI는 홈 화면 구조의 재사용입니다.** 쇼핑몰 카테고리 트리를 만들지 말고,
**검색이 주인공 + 얕은 카테고리 + 필터 조합**으로 갑니다. `buildTabOptions`(데이터에서
탭 생성, 많은 순 N개 + 더보기)와 `query.ts`(AND 교집합, 건수)가 이미 그 일을 합니다.
공개 인덱스에 같은 파이프라인을 겨누면 되고, 새로 필요한 건 정렬뿐입니다.
**카테고리 하드코딩은 하지 않습니다** — V2에서 이미 졸업한 문제입니다.

**성공 지표는 커버리지와 신선도입니다.** 능동 검색은 실패가 곧 이탈입니다. 특히
**끝난 공구가 뜨면 가격이 조금 틀린 것보다 치명적**인데, 마감일 정확도는 AI 추출에
달려 있습니다(`userDeadline` 교정 필드가 있는 것 자체가 AI가 틀린다는 증거).
개인 시렁에서 틀린 건 본인이 고치면 되지만 공개 검색은 기준이 다릅니다.

**랭킹은 여러 신호를 씁니다.** 저장 수는 **중요한 신호 중 하나**일 뿐입니다 —
특정 인플루언서 팬이 몰렸거나 프로모션으로 튄 것일 수 있습니다.
저장 수 + 클릭 + 최근성 + 마감 임박 + 정보 신뢰도를 같이 봅니다.

**UX는 공유 컬렉션과 반대입니다.** 컬렉션은 개인 목록 위에 얹는 축이지만,
공구는 *남들 것 · 서버 · 공개*라 **다른 세계**입니다. 섞으면 "전체 = 내가 저장한
기억"이 깨집니다. 별도 공간이 정당하고, 두 세계를 잇는 건 `[내 시렁에 담기]` 하나입니다.

**지뢰.** 남의 이미지·캡션 재게시는 문제가 됩니다. 공개 DB는 **facts 중심으로 최소화**하고
(누가 저장했는지는 드러나지 않음), 이미지와 원문은 인플루언서가 claim한 뒤 본인이 올린
것만 씁니다. **"원래 공개된 정보"라는 것이 곧 "재사용해도 된다"는 뜻은 아닙니다** —
계정명·판매 링크·반복 수집은 성격이 다릅니다.

- [ ] **공개 서비스 전에 별도 검토**: 원본 플랫폼 이용정책, 저작권, 개인정보,
      데이터 재사용 범위

**순서는 앞당겨지지 않습니다.** 이 전략도 사용자가 어느 정도 있어야 작동합니다.
다만 사람을 모으는 이유가 공구가 아니라 개인 수집함이고, 그 부산물로 공구 DB가
쌓입니다. `프록시 → 공유 → 사용자 확보 → 공구` 그대로입니다.

## iOS

- [ ] Apple Developer 등록 (App Group entitlement의 전제)
- [ ] Share Extension 켜기 — `disableIOS: false` + `iosAppGroupIdentifier` + prebuild
- [ ] EAS credentials에서 extension 타겟 설정
- [ ] 실기기 확인

> 참고: 지금 아이폰 웹(PWA)은 **공유 시트에 뜰 수 없습니다.** 사파리가 Web Share Target을
> 지원하지 않아서, 네이티브 앱이 나오기 전까지 복사→붙여넣기가 유일한 경로입니다.

## 실기기 확인 (미완)

- [ ] 안드로이드 공유 인입 실기기 테스트
- [ ] 인스타 DM 링크 길게 누르기 / 공유 / 복사 흐름 실기기 확인
- [ ] `SearchFilterBar` 칩 2단 구성이 세로로 너무 커지지 않는지

## 판단 필요 — 할지 말지 정하기

- [ ] `status`(inbox / to_read / done / archived) — PRD에만 있고 코드에 흔적 없음. 만들지 버릴지
- [ ] `tags` — 없음. facet이 사실상 대신하고 있어 **버리는 쪽**이 맞아 보임
- [ ] source type 필터 — 탭이 분야 기준이라 출처 축은 없음. 필요한지

## 알려진 거칠음

- [ ] `search.ts` 자소 부분 매칭이 느슨함 (초성 `ㄱㅈ`가 '허벅지'에도 걸림)
- [ ] 자유 텍스트 지역·시설 스캔이 단순 포함 검사라 오탐 여지

## 기반

- [ ] ESLint / Prettier 설정
- [ ] 저장 실패 케이스 처리

## 문서 정합성 (코드와 어긋나 있음)

- [ ] `Engineering_Spec_AI_Note_App.md` — "온디바이스 AI only, 클라우드 AI 금지"로 적혀 있으나
      실제는 Gemini 클라우드. **소형 온디바이스 AI 검토는 접은 것으로 확정**
- [ ] `AI_Functional_Spec.md` — 같은 문제 + 모델 크기/성능 목표가 현재와 무관
- [ ] `PRD_v1_AI_Note_App.md` — 데이터 모델이 실제 스키마(조각/사전/V2 content)와 다름
