# 작업 보고 — 조합 검색 / 냉장고 털기

작업일: 2026-09-02
범위: "정보를 모으는 게 기본, 조건 조합으로 꺼내는 건 그 위에 얹는 레이어"

---

## 1. 무엇이 달라졌나

### 기본기 복구 — 저장한 건 반드시 찾을 수 있다
검색이 제목·메모·구조화 필드만 보고 있었습니다. DM 원문에 "감자"가 있어도
AI 분류가 실패한 아이템은 검색에서 사라졌습니다.

원문(`rawInput`) · 본문(`description`) · URL · 요약을 검색 대상에 추가했습니다.
성능 때문에 길이로 전략을 나눕니다. 300자 이하는 초성·퍼지까지, 그 이상은 단순 포함 검사만.
`hangulMatch`가 대상 전체를 자모 분해하는 구조라 수만 자 본문에 매 타이핑마다 걸면 입력이 밀립니다.

`getItemCategory`는 `parenting`을 반환하는데 탭 목록엔 없어서 육아 아이템이
'전체'에서만 보였습니다. 탭을 추가하고, 개수가 늘어도 글자가 눌리지 않게 가로 스크롤로 바꿨습니다.

### 조합 검색 — "국내 + 수영장 + 강원도"
`activeKeyword`가 단수 string이라 칩을 누르면 이전 게 교체됐습니다. AND 조합이 불가능했습니다.
`selectedFacets: string[]`로 바꾸고 교집합 질의를 붙였습니다.

핵심은 **값 정규화**입니다. `강릉`과 `강원도`가 다른 값이면 조합은 헛돕니다.
아이템에 상위 개념을 미리 붙여두는 방식(`강릉` → `강원도` → `국내`)을 택했습니다.
질의 쪽은 특별한 처리 없이 단순 교집합만으로 계층 검색이 됩니다.

### UI/UX — 배우지 않고도 쓰게
- **죽은 칩을 숨깁니다.** 누르면 0건이 되는 칩은 아예 안 보입니다. 대신 각 칩에 "누르면 몇 건"을 표시합니다.
- **검색어가 칩이 됩니다.** "강원도 수영장"이라고 치면 칩 두 개로 승격됩니다.
  정확히 일치할 때만, 그리고 입력 중인 마지막 낱말은 건드리지 않습니다(한글 조합이 끊기지 않게).
- **0건일 때 원인을 짚어줍니다.** "결과 없음" 대신 조건을 하나씩 빼봤을 때의 건수를 계산해
  바로 누를 수 있게 제시합니다. 원인이 조건이 아니라 검색어일 때도 마찬가지입니다.

### 냉장고 털기 — 방향을 뒤집은 조회
기존은 [레시피 → 재료로 거르기]. 이건 [가진 재료 → 만들 수 있는 것 순위]입니다.
완전히 갖춘 것만 보여주면 실제로 거의 안 걸리므로 부족 2개까지 포함하고,
"이거 하나 사면 N개가 열린다"를 상단에 얹었습니다. 마트에서 쓰는 화면입니다.

---

## 2. 파일

| 파일 | 역할 |
|---|---|
| `src/features/facets/normalize.ts` | 동의어·표기 통일, 지역/재료/부위 계층 사전 |
| `src/features/facets/extract.ts` | 아이템 → facet 추출 (구조화 필드 + 자유 텍스트 스캔) |
| `src/features/facets/query.ts` | 인메모리 역색인, AND 교집합, 건수 계산, 완화 제안 |
| `src/features/facets/pantry.ts` | 보유 재료 → 레시피 랭킹 |
| `src/features/facets/parseQuery.ts` | 검색어 → 칩 승격 |
| `src/features/facets/labels.ts` | 화면 표시용 라벨/이모지 |
| `src/components/EmptyResultGuide.tsx` | 0건 화면 + 완화 제안 |
| `src/components/PantryModal.tsx` | 냉장고 털기 |
| `src/components/SearchFilterBar.tsx` | 다중 선택 + 건수 표시로 교체 |
| `src/screens/HomeScreen.tsx` | 필터 파이프라인 배선 |
| `src/utils/formatters.ts` | 원문 폴백 검색 추가, 단일 키워드 필터 제거 |

### 왜 SQLite 테이블이 아니라 인메모리인가
store가 이미 전체 아이템을 들고 있어 마이그레이션과 sync 스키마 변경 없이 같은 결과를 얻습니다.
수천 건까지는 교집합 연산이 체감되지 않습니다.
더 커지면 `query.ts`의 함수 시그니처를 유지한 채 내부만 SQL로 내리면 됩니다.

---

## 3. 검증한 것 / 못 한 것

**검증함**
- `npm run typecheck` 통과
- `npx expo export --platform web` 번들 성공 (342 modules)
- 필터 파이프라인 통합 테스트 (HomeScreen과 동일한 순서로 재현)
  - 조합 AND, 텍스트+조합 동시 적용, 분류 실패 아이템 검색, 0건 완화 제안,
    죽은 칩 배제, 칩 승격, pantry 매칭 및 상위개념 인정

**못 함**
- 실기기/에뮬레이터 실행. 시각적 레이아웃, 터치 영역, 스크롤 감각은 확인 못 했습니다.
  특히 `SearchFilterBar`의 칩 2단 구성(선택된 조건 / 추천 조건)은 실제로 보고 조정이 필요합니다.

---

## 4. 남은 것

**바로 확인 필요**
- 실기기에서 칩 영역이 세로로 너무 커지지 않는지. 필요하면 추천 칩 개수(`MAX_SUGGESTED_CHIPS = 12`)를 줄이면 됩니다.

**기능 공백**
- 육아 카테고리는 추천 칩이 안 뜹니다. `service.ts` 프롬프트에 recipe/workout/travel 엔티티만 정의돼 있어서입니다.
  월령·지원금 종류·준비물 같은 걸 정하면 붙습니다.
- pantry 보유 재료가 앱을 닫으면 사라집니다. `expo-sqlite`에 settings 테이블을 두는 게 자연스럽습니다.

**알려진 거칠음**
- `search.ts`의 자소 부분 매칭이 느슨합니다. 초성 "ㄱㅈ"가 "허벅지"에도 걸립니다
  (자모 분해 후 순서 매칭 규칙 때문). 기존 동작이라 이번엔 건드리지 않았습니다.
- 자유 텍스트 지역/시설 스캔은 단순 포함 검사라 오탐 여지가 있습니다.
  여행 정보는 표현이 자유로워 정확한 파싱보다 회수율이 높은 쪽을 택했습니다.

**보안**
- `.env`가 gitignore에 없어 `git add -A` 한 번에 Gemini 키가 커밋될 수 있었습니다. `.env` 항목을 추가했습니다.
- `EXPO_PUBLIC_` 키는 번들에 평문으로 박힙니다. 출시 전 Supabase Edge Function 프록시가 필요합니다.

---

## 5. 커밋하지 않았습니다

전부 워킹 트리에 있습니다. `git diff`로 확인하고 직접 커밋하시면 됩니다.
